#build docker ftp image
> docker build -t alpine-ftp-python -f ftp_server/Dockerfile .

#run docker ftp image
> docker run -p 2021:21 -p 60000-60010:60000-60010 alpine-ftp-python

#connect to ftp server
> ftp localhost 2021 
> -u anonymous -P '' 

#recursive delelte all files 
> find . -type f -exec rm {} \;

#docker ps to check if ftp server is running and get container id
> docker ps

#get into ftp server using bash
> docker exec -it <container-id> bash

#change permsions to /data dirs
> chmod -R 777 staging_area/ retention_area/

#fun file_watcher
> python3 file_watcher.py 

#for streaming layer
> hdfs dfs -mkdir -p /user/itversity/kafka_data
> hdfs dfs -mkdir -p /user/itversity/checkpoints
> hdfs dfs -chmod -R 777 /user/itversity